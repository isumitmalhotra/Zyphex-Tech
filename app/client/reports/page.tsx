import ReportsPage from '@/app/user/reports/page';

// Client reports page - uses same component as user reports
// Clients see only reports that are shared with them or marked as public
// Access control is handled via role-based permissions in the API

export default ReportsPage;
